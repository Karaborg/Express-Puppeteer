html
  head
    meta(charset='utf-8')
    meta(http-equiv='X-UA-Compatible', content='IE=edge')
    title Screenshot
    link(rel='stylesheet', href='/stylesheets/mycss.css')
    link(rel='stylesheet', href='/toastr/build/toastr.css')
    script(src='https://ajax.googleapis.com/ajax/libs/jquery/3.3.1/jquery.min.js')
    // Tell the browser to be responsive to screen width
    meta(content='width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no', name='viewport')

    body
        .background
          .container
              .screen
                div
                  button.button#btnAdd Add URL
                  div
                    table#table(style='width: 100%')
                  button.button#btnScreenShot Take the Screenshot
                  button.button#btnDownload Download
                  //div#download 
    //jquery
    script(src='https://code.jquery.com/jquery-3.4.1.min.js')
    script(src='toastr/toastr.js')
    script.
      var s = 0;
      var data;
      var array = [];

      $('#table').append('<input id=' + "url_" + s + ' placeholder="' + " Enter URL to Screenshot" + '", class="input"></input><input id=' + "dn_" + s + ' placeholder="' + " Delay #" + '", type="' + "number" + '", class="inputb"></input>')
      s++;

      $('#btnAdd').click(function (e) {
        e.preventDefault();
        $('#table').append('<input id=' + "url_" + s + ' placeholder="' + " Enter More URL to Screenshot" + '", class="input"></input><input id=' + "dn_" + s + ' placeholder="' + " Delay #" + '", type="' + "number" + '", class="inputb"></input>')
        s++;
      })
      $('#btnScreenShot').click(function (e) {
        e.preventDefault();
        data = {
          lenght: s
        }
        for(i = 0; i < s; i++){
          var doc = document.getElementById('url_' + i).value;
          array.push(doc);
          if(doc){
            toastr.info("Screenshot for: " + doc);
          }else{
            toastr.info("Empty Input");
          }
          data['url_' + i] = array[i]
        }
        $.post('/hey', data)
                  .done(function (returnValue) {
                    if(returnValue){
                      console.log("Success")
                      toastr.info("Finished");
                    }
                  })
      })
      $('#btnDownload').click(function (e) {
          e.preventDefault();
              $.post('/heyy')
                  .done(function (returnValue) {
                    console.log(returnValue)
                    if(returnValue){
                      console.log("Downloaded!")
                      toastr.info("Downloaded!");
                    }else{
                      toastr.warning("No File to Download")
                    }
                  })
          })